#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h> 

int main() {

	/*
	int n;
	int arr[100]; //미리 많이 할당해둔다.

	printf("입력할 숫자 개수 입력 : ");
	scanf("%d", &n);

	for (int i = 0; i < n; i++) 
		scanf("%d", &arr[i]);

	// 배열을 반대로 출력
	for (int i = n - 1; i >= 0; i--)
		printf("%d ", arr[i]);
		*/





	//최댓값을 구하는 코드 (최솟값은 이 코드와 반대로 구현)
	/*
	int n;
	int arr[100];

	scanf("%d", &n);
	for (int i = 0; i < n; i++) scanf("%d", &arr[i]);
	
	int max = arr[0];
	for (int i = 1; i < n; i++) {
		if (max < arr[i]) max = arr[i];
	}
	printf("%d\n", max);
	*/

	
}